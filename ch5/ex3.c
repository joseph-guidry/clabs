#include <stdio.h>

#define MAX 100

int main(void)
{
	int c, i;
	char array[MAX] = "Abcedfghaisklmnopqrstuvwxyzsdfadf;
	while((c = getchar()) != EOF){
		array[i++] = c;
		if( c == '\n')
			if (i < 20)
				for (int x = 0; x < count; x++){
					putchar(line[x]);
				}	
	}
}
